# 2026-08-23 — the tournament day: index of record

Karim's directive: *"really make use of TradingView and running strategies and
backtests on it… most important thing is no overfitting… I want the strategies
to be really good."* This file indexes everything that answered it.

## Deliverables (all in this repo)

| artifact | where |
|---|---|
| Strategy-backtest MCP surface (3 scoped tools, double-gated) | `src/core/backtest.js`, `src/tools/backtest.js`, `src/allowlist.js` (`backtest` preset) |
| 14-pass sol-max review ledger | `docs/PILOT_RESULT_2026-08-23.md` |
| Anti-overfitting protocol P1–P7 | `docs/ANTI_OVERFITTING_PROTOCOL.md` |
| Tournament harness / validator / report generator | `scripts/backtest-tournament.mjs`, `scripts/validate-tournament-run.mjs`, `scripts/tournament-report.mjs` |
| The validated 105-cell run + report | `research/tournament-2026-08-23/run-2026-08-23/` (`TOURNAMENT_REPORT.md`) |
| TV-EV-01 prereg + verdict (FAIL 0/5 → harness fault → FAIL 3/5) | `docs/PREREG_TV-EV-01.md`, `research/tournament-2026-08-23/tv-ev-01*/` |
| TV-EV-02 prereg + verdict (FAIL 2/5, instrument faults) | `docs/PREREG_TV-EV-02.md`, `research/tournament-2026-08-23/tv-ev-02/` |
| Replay walkers | `scripts/tv-ev-01-walk.mjs`, `scripts/tv-ev-02-walk.mjs` |

Evidence screenshots (~125MB) are deliberately git-ignored; cell JSONs carry
their paths on asus15. Estate registry entries: claude-mubasher PRs #213–#217.

## The results, one paragraph each

**Tournament:** none of karim's 7 strategies earns a rank. Daily trio:
INSUFFICIENT-EVIDENCE (median 5–12 trades over ~24 years). Intraday trio:
uniformly losing (both ORHDs negative on all 8 liquid symbols, WKOL on all 8).
ABUK: zero trades even on ABUK. The prize: the per-symbol SMA100 gate at 100%
equity shows **~20.6%/yr raw IS position return — independently corroborating
the live basket's ≈19%/yr** via a different engine and data path.

**TV-EV replay series:** two pre-registered studies, two honest fails, zero
bent tolerances. EV-01: walker filled one bar early (15/15 unanimous), fixed
to the frozen Pine's own semantics → 3/5. EV-02: the registered dust constant
was arithmetically naive (true value −0.001998002 = the commission round-trip
formula) and the walker proved NONDETERMINISTIC (TMGH trade lists differ on
identical inputs — bar-reveal reads race replay_step) → 2/5. Series PAUSED:
a deterministic step-and-confirm bar reveal is a reviewed engineering task
prerequisite to any TV-EV-03. Deterministic core across both walks: COMI/ORAS
≤0.012%, monster trades byte-identical — substantively supportive of
no-lookahead, formally unresolved, instrument-limited.

## Security shape that shipped

Double gate (`TV_MCP_ALLOW_SCOPED_WRITERS=1` AND allowlist naming);
attestation of the facade implementations pinned by source hash and bound
atomically to the invoked callables; draft-only attach (a TV *draft* is
persisted — identical to a manual "Add to chart"; saved scripts never written,
inventory pinned at 12 across every run); CRLF-canonical source-digest
identity; series-fingerprint ownership with structural corporate-action
handling; no DOM click reachable from the backtest surface.
